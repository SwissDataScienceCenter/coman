#[derive(Debug, Eq, PartialEq, Clone, Hash)]
pub enum Id {
    Toolbar,
    WorkloadList,
    WorkloadLogs,
    GlobalListener,
    Menu,
    InfoPopup,
    ErrorPopup,
    LoginPopup,
    SystemSelectPopup,
    FileView,
}
